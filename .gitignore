# Claude Code local/personal settings (keep .claude/settings.json shareable if added later)
.claude/settings.local.json
.claude/*.tmp.*
.claude/**/*.tmp.*

# Python
__pycache__/
*.py[cod]
.venv/
venv/
.uv/
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# PROJECT DUET generated artifacts (reproducible via `make all`; never committed)
runs/                      # RL training runs / TensorBoard events / checkpoints
orchestrator/heartbeat.log
orchestrator/stages.state.json
weekend.out
*.onnx
*.stl
*.3mf
*.usd
*.usda
docs/reports/
docs/ingest/

# OS / editor cruft
.DS_Store
*.swp
