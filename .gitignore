# Claude Code local/personal settings (keep .claude/settings.json shareable if added later)
.claude/settings.local.json
.claude/*.tmp.*
.claude/**/*.tmp.*

# Python
__pycache__/
*.py[cod]
.venv/
venv/
.uv/
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# PROJECT DUET generated artifacts (reproducible via `make all`; never committed)
# RL training runs / TensorBoard events / checkpoints
runs/
orchestrator/heartbeat.log
orchestrator/stages.state.json
orchestrator/.env_ready
orchestrator/.isaac_image_digest
# per-track failure files (regenerated each run)
FAILURE-*.md
weekend.out
*.onnx
*.stl
*.3mf
*.usd
*.usda
*/description/*.urdf
*/description/*.mjcf.xml
docs/reports/
docs/bom/

# OS / editor cruft
.DS_Store
*.swp

# Vendored upstream (fetch via scripts/fetch_upstream.sh; pinned SHAs)
third_party/

# Hydra run outputs (training runtime; not source)
outputs/

.venv-mjlab/
